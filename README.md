# Browser-Automation-Agent
A production-grade browser automation agent using OpenAi Vision + Playwright  that handles real-world failure modes: popup interrupts, layout shifts,  auth walls, and confirmation gates for destructive actions.
